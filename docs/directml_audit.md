# DirectML Compatibility Audit

Automatically generated by `scripts/audit_directml.py`.

> **M4 note:** trainer precision handling, bounded `--max_steps`
> execution, and shared DirectML FP16 utilities changed after this
> snapshot. Regenerate this report from the current source tree before
> using the finding counts as the final M4/M5 audit.

> This report identifies potential CUDA-specific code. Each occurrence
> must still be reviewed manually before modification.

## Summary

Total findings: **86**

  Category             Count
  ------------------ -------
  AMP                     29
  CUDA reference          15
  CUDA utility             1
  Device placement         2
  Distributed             39

## Findings

  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  File                                           Line Type          Action        Code
  --------------------------------- ----------------- ------------- ------------- -------------------------------------------------------------------------------------------------------------------------------------------------------
  `trainer/rollout_engine.py`                      13 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/rollout_engine.py`                      66 AMP           Investigate   `def __init__(self, policy_model: torch.nn.Module, tokenizer, device: str = "auto", autocast_ctx=None):`

  `trainer/rollout_engine.py`                      70 AMP           Investigate   `self.autocast_ctx = autocast_ctx`

  `trainer/rollout_engine.py`                      74 AMP           Investigate   `ctx = self.autocast_ctx if self.autocast_ctx else nullcontext()`

  `trainer/rollout_engine.py`                     215 AMP           Investigate   `autocast_ctx = None,`

  `trainer/rollout_engine.py`                     221 AMP           Investigate   `return TorchRolloutEngine(policy_model, tokenizer, device, autocast_ctx)`

  `trainer/train_agent.py`                         18 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_agent.py`                        276 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_agent.py`                        416 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_agent.py`                        417 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_agent.py`                        427 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_agent.py`                        449 AMP           Investigate   `autocast_ctx=autocast_ctx,`

  `trainer/train_agent.py`                        478 Distributed   Investigate   `model = DistributedDataParallel(model, device_ids=[local_rank])`

  `trainer/train_agent.py`                        486 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"), collate_fn=collate_fn)`
                                                      reference                   

  `trainer/train_distillation.py`                  13 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_distillation.py`                  73 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_distillation.py`                 197 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_distillation.py`                 198 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_distillation.py`                 210 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_distillation.py`                 230 AMP           Investigate   `scaler = torch.cuda.amp.GradScaler(enabled=(device_type == "cuda" and args.dtype == 'float16'))`

  `trainer/train_distillation.py`                 249 Distributed   Investigate   `model = DistributedDataParallel(model, device_ids=[local_rank])`

  `trainer/train_distillation.py`                 257 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"))`
                                                      reference                   

  `trainer/train_dpo.py`                           13 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_dpo.py`                           73 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_dpo.py`                          160 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_dpo.py`                          161 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_dpo.py`                          172 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_dpo.py`                          194 AMP           Investigate   `scaler = torch.cuda.amp.GradScaler(enabled=(device_type == "cuda" and args.dtype == 'float16'))`

  `trainer/train_dpo.py`                          213 Distributed   Investigate   `model = DistributedDataParallel(model, device_ids=[local_rank])`

  `trainer/train_dpo.py`                          221 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"))`
                                                      reference                   

  `trainer/train_full_sft.py`                      12 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_full_sft.py`                      35 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_full_sft.py`                     112 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_full_sft.py`                     113 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_full_sft.py`                     124 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_full_sft.py`                     139 AMP           Investigate   `scaler = torch.cuda.amp.GradScaler(enabled=(device_type == "cuda" and args.dtype == 'float16'))`

  `trainer/train_full_sft.py`                     158 Distributed   Investigate   `model = DistributedDataParallel(model, device_ids=[local_rank])`

  `trainer/train_full_sft.py`                     166 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"))`
                                                      reference                   

  `trainer/train_grpo.py`                          15 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_grpo.py`                          98 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_grpo.py`                         248 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_grpo.py`                         249 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_grpo.py`                         261 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_grpo.py`                         287 AMP           Investigate   `autocast_ctx=autocast_ctx,`

  `trainer/train_grpo.py`                         318 Distributed   Investigate   `model = DistributedDataParallel(model, device_ids=[local_rank])`

  `trainer/train_grpo.py`                         327 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"))`
                                                      reference                   

  `trainer/train_lora.py`                          12 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_lora.py`                          36 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_lora.py`                         106 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_lora.py`                         107 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_lora.py`                         118 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_lora.py`                         152 AMP           Investigate   `scaler = torch.cuda.amp.GradScaler(enabled=(device_type == "cuda" and args.dtype == 'float16'))`

  `trainer/train_lora.py`                         169 Distributed   Investigate   `model = DistributedDataParallel(model, device_ids=[local_rank])`

  `trainer/train_lora.py`                         177 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"))`
                                                      reference                   

  `trainer/train_ppo.py`                           13 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_ppo.py`                          173 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_ppo.py`                          357 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_ppo.py`                          358 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_ppo.py`                          369 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_ppo.py`                          399 AMP           Investigate   `autocast_ctx=autocast_ctx,`

  `trainer/train_ppo.py`                          434 Distributed   Investigate   `actor_model = DistributedDataParallel(actor_model, device_ids=[local_rank])`

  `trainer/train_ppo.py`                          435 Distributed   Investigate   `critic_model = DistributedDataParallel(critic_model, device_ids=[local_rank])`

  `trainer/train_ppo.py`                          444 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"))`
                                                      reference                   

  `trainer/train_pretrain.py`                      12 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/train_pretrain.py`                      35 AMP           Investigate   `with autocast_ctx:`

  `trainer/train_pretrain.py`                     115 Distributed   Investigate   `local_rank = init_distributed_mode(args.device)`

  `trainer/train_pretrain.py`                     116 Distributed   Investigate   `if dist.is_initialized(): args.device = get_device(f"cuda:{local_rank}")`

  `trainer/train_pretrain.py`                     127 AMP           Investigate   `autocast_ctx = torch.cuda.amp.autocast(dtype=dtype) if device_type == "cuda" else nullcontext()`

  `trainer/train_pretrain.py`                     142 AMP           Investigate   `scaler = torch.cuda.amp.GradScaler(enabled=(device_type == "cuda" and args.dtype == 'float16'))`

  `trainer/train_pretrain.py`                     161 Distributed   Investigate   `model = DistributedDataParallel(model, device_ids=[local_rank])`

  `trainer/train_pretrain.py`                     169 CUDA          Review        `loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=(device_type == "cuda"))`
                                                      reference                   

  `trainer/trainer_utils.py`                       12 Distributed   Investigate   `import torch.distributed as dist`

  `trainer/trainer_utils.py`                       36 CUDA          Review        `if torch.cuda.is_available():`
                                                      reference                   

  `trainer/trainer_utils.py`                       37 Device        Adapt         `return torch.device("cuda")`
                                                      placement                   

  `trainer/trainer_utils.py`                       68 CUDA          Review        `if device.type == "cuda":`
                                                      reference                   

  `trainer/trainer_utils.py`                       69 CUDA utility  Investigate   `torch.cuda.empty_cache()`

  `trainer/trainer_utils.py`                      107 CUDA          Review        `if device.type != "cuda":`
                                                      reference                   

  `trainer/trainer_utils.py`                      108 CUDA          Review        `raise RuntimeError(f"Distributed training is currently supported only with CUDA, got device: {device}.")`
                                                      reference                   

  `trainer/trainer_utils.py`                      110 Distributed   Investigate   `dist.init_process_group(backend="nccl")`

  `trainer/trainer_utils.py`                      111 Distributed   Investigate   `local_rank = int(os.environ["LOCAL_RANK"])`

  `trainer/trainer_utils.py`                      112 Distributed   Investigate   `torch.cuda.set_device(local_rank)`

  `trainer/trainer_utils.py`                      113 Distributed   Investigate   `return local_rank`

  `trainer/trainer_utils.py`                      120 CUDA          Review        `if torch.cuda.is_available():`
                                                      reference                   

  `trainer/trainer_utils.py`                      121 CUDA          Review        `torch.cuda.manual_seed(seed)`
                                                      reference                   

  `trainer/trainer_utils.py`                      122 CUDA          Review        `torch.cuda.manual_seed_all(seed)`
                                                      reference                   

  `trainer/trainer_utils.py`                      225 Device        Adapt         `def __init__(self, model_path, device="cuda", dtype=torch.float16):`
                                                      placement                   
  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

## Action meanings

-   **Adapt** --- expected to require a DirectML-compatible
    implementation.
-   **Investigate** --- behavior or DirectML support must be tested
    before changing it.
-   **Review** --- inspect manually; the occurrence may be harmless.

## Recommended review order

1.  Device placement
2.  CUDA utilities
3.  AMP / mixed precision
4.  Distributed training
5.  Precision options
6.  Generic CUDA references
